node_modules/
dist/
.output/
build/
.turbo/
coverage/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local

# Electron
apps/desktop/out/
apps/desktop/dist/

# Expo
apps/mobile/.expo/
apps/mobile/ios/
apps/mobile/android/

# Regenerated by plugins/withCredentialsProviderPod.js on every `expo
# prebuild` — the vendored_frameworks path inside is a machine-specific
# node_modules resolution, not portable source.
apps/mobile/targets/credentials-provider/ClibsodiumXCFramework.podspec

# Supabase local
supabase/.branches/
supabase/.temp/

# WXT (browser extension) generated types/config — regenerated by `wxt prepare`
apps/browser-extension/.wxt/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# Claude Code local tooling — cached skill definitions and local-only
# settings, not project source (per Claude Code convention, "local"
# settings files stay untracked, same as .env.local).
.claude/settings.local.json
.claude/skills/
.agents/
skills-lock.json
